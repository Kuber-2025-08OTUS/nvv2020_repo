apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: nfs-storage
provisioner: kubernetes.io/no-provisioner  # Для ручного создания PV
volumeBindingMode: WaitForFirstConsumer
# mountOptions:
#   - fsGroup=1001